# Session Storage Configuration

# Set to true to enable SQLite persistent storage for sessions
# If not set or set to false, sessions will be stored in memory only
USE_SQLITE_SESSION=false

# Note: When using SQLite, sessions will persist across server restarts
# The database file will be created as 'sessions.db' in the application directory
